name: Deploy Node JS

on:
  push:
    branches: ["main"]
  workflow_dispatch:

env:
  PUBLISH_URL: ${{ secrets.LIN_CLOUD_URL_240 }}
  PUBLISH_KEY: ${{ secrets.LIN_CLOUD_KEY_240 }}
  ZIP_NAME: app.zip

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      # --------------------
      # Checkout
      # --------------------
      - name: Checkout
        uses: actions/checkout@v4

      # --------------------
      # Zip dist (Excluyendo archivos innecesarios para un paquete más ligero)
      # --------------------
      - name: Zip project folder
        run: |
          zip -r ${ZIP_NAME} . -x "node_modules/*" ".git/*" ".env" "test-db.js"

      # --------------------
      # Deploy
      # --------------------
      - name: Upload ZIP a LIN Cloud IIS Push
        run: |
          curl -X POST \
            "$PUBLISH_URL/api/Publish?createRelease=true" \
            -H "accept: text/plain" \
            -H "Content-Type: multipart/form-data" \
            -H "Key: $PUBLISH_KEY" \
            -F "file=@${ZIP_NAME}"
